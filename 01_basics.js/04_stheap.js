// stack(primitive),heap(non-primitive)
//  for stack we only get the copy but for heap we get the refernce of the prevbious one
// so when we change the value in stack we can change the copy value not the orginal one but in heap we change the whole value for the iniotial ojnme also

let myutube="sameer"

let another="mummy"
another="someone"

console.log(myutube);
console.log(another);

